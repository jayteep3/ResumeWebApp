<html>
<head>
    <title>Update <%= account.email%></title>
</head>
<body>
<h1>Update <%= account.email%></h1>
<form action="/account/update" method="get">
    <input type="hidden" id="account_id" name="account_id" value="<%= account.account_id %>">
    <!--need to fix this-->
    Account: <input id="email" name="first_name" type="text" value="<%= account.first_name%>"/>
    <br />
    <br />
    Address
    <select id="address_id" name="address_id">
        <% for(var i=0; account.length > i; i++ ) {
            var selected = "";
            //need to change this line
            if(account[i].account_id == school.school_id) {
                selected = "selected";
            }
        %>
        <option value="<%= account[i].account_id %>" <%= selected %>><%= account[i].email + ', ' + account[i].first_name + ', ' + account[i].last_name %></option>
        <% } %>
    </select>

    <br />

    <input type="submit" />

</form>
</body>

</html>